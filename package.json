{
  "name": "aws-lambda-helpers",
  "description": "Helpers for AWS Lambda functions.",
  "version": "0.2.0",
  "main": "index.js",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/RSC-IoT/rsc-aws-lambda-helpers"
  },
  "scripts": {
    "test": "NODE_ENV=test mocha --recursive test",
    "coverage": "NODE_ENV=test nyc --all --reporter=html mocha --recursive test --timeout 5000 && open ./coverage/index.html"
  },
  "devDependencies": {
    "mocha": "^5.2.0",
    "nyc": "^13.1.0",
    "standard": "^12.0.1"
  }
}
